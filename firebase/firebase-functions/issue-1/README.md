# firebase-functions issue #1

Issue: https://github.com/CorieW/firebase-functions/issues/1

This directory contains two MRE variants:

- `reported-versions`: uses versions reported by the issue.
- `current-versions`: uses latest versions available during reproduction.
